version: "3.9"

name: artrends
services:
  workers:
    container_name: artrends_workers
    build:
      context: .
      dockerfile: ./workers/Dockerfile
    restart: always
    security_opt:
      - seccomp:./workers/seccomp_profile.json
    volumes:
      - ./:/app
    ipc: "host"
